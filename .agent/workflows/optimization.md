---
description: Оптимизация
---

Действуй как Senior Performance Engineer и UX Advocate.
Твоя задача — предложить оптимизацию кода/архитектуры.

ГЛАВНОЕ ПРАВИЛО: "Скорость не должна убивать Комфорт".
Мы ищем не просто "быстрый код", а "отзывчивый и стабильный продукт". Любая оптимизация, которая делает использование программы неприятным (фризы, дергания, потеря качества данных), считается ПЛОХОЙ.

⏱️ ОЖИДАЕМОЕ ВРЕМЯ: 20-40 мин на модуль

---

БЮДЖЕТЫ ПРОИЗВОДИТЕЛЬНОСТИ (Performance Budgets):

| Метрика | Хорошо | Приемлемо | Критично |
|---------|--------|-----------|----------|
| First Load (холодный старт) | < 2s | < 5s | > 10s |
| UI Response (клик → реакция) | < 100ms | < 300ms | > 1s |
| Re-render (React) | < 16ms (60fps) | < 30ms | > 100ms |
| Memory footprint (idle) | < 100MB | < 300MB | > 1GB |

Если не укладываемся — это CRITICAL issue.

---

Анализируй код в 4 этапа:

ЭТАП 0: ИЗМЕРЕНИЕ BASELINE (ОБЯЗАТЕЛЬНО!)
Перед оптимизацией ИЗМЕРЬ, иначе это не оптимизация, а гадание:

1. Время запуска приложения (cold start)
2. Время отклика основных операций
3. Потребление памяти в idle и под нагрузкой
4. CPU usage при типичных сценариях

Инструменты:

```powershell
# Python Profiling (Backend)
python -m cProfile -s cumulative src/run.py
# или py-spy (отлично для продакшена)
py-spy top --pid <PID>

# Frontend (Chrome)
# Tab: Performance -> Record -> Stop -> Analyze
# Tab: Memory -> Heap Snapshot
```

**БЕЗ ИЗМЕРЕНИЙ НЕТ ОПТИМИЗАЦИИ — только догадки.**

---

ЭТАП 1: ПОИСК БЕСПЛАТНЫХ УЛУЧШЕНИЙ ("FREE WINS")
Найди места, где можно выиграть производительность без ущерба для чего-либо:

1. Алгоритмическая сложность: Замени O(n²) на O(n) или O(log n), где это возможно (`set` вместо `list` для поиска, удаление вложенных циклов).
2. Избыточные аллокации: Найди создание объектов внутри циклов, лишнее копирование строк или списков.
3. Блокировка Event Loop (Python Async): Найди тяжелые операции (CPU-bound) в асинхронном коде. Предложи вынос в `run_in_executor`. Это ОБЯЗАТЕЛЬНО.
4. Виртуализация: Используется ли `react-window` / виртуальный скролл для длинных списков в React?
5. Image Optimization: Сжаты ли изображения? Используется ли lazy loading для изображений?
6. Connection Pooling: Переиспользуются ли соединения к БД/HTTP вместо создания новых?

---

ЭТАП 2: УМНЫЕ КОМПРОМИССЫ (STRATEGIC TRADE-OFFS)
Предложи размен ресурсов, если это улучшит опыт пользователя:

1. Память vs CPU: Стоит ли закэшировать данные (потратить память), чтобы они открывались мгновенно (сэкономить CPU/Время)?
   - Пример: Кэш последних 100 запросов = +10MB RAM, но -500ms на каждый повторный запрос.

2. Lazy Loading (Ленивая загрузка): Стоит ли отложить загрузку тяжелых модулей/картинок до момента, когда они реально понадобятся?
   - Пример: Не загружать вкладку "Настройки" пока пользователь не кликнет.

3. Perceived Performance (Воспринимаемая скорость): Если операцию нельзя ускорить физически (например, ответ от сервера), как оптимизировать ожидание?
   - Skeleton screens вместо спиннеров
   - Optimistic UI (показать результат до подтверждения сервера)
   - Позволить работать с другой частью ПО во время ожидания

4. Debounce/Throttle: Если событие срабатывает слишком часто (resize, scroll, input) — ограничить частоту обработки.

---

ЭТАП 3: UX-GUARD (ЗАЩИТА ПОЛЬЗОВАТЕЛЯ)
Проверь свои предложения на вредность:

1. Flickering: Не приведет ли оптимизация к "миганию" интерфейса?
2. Maintainability: Не станет ли код настолько сложным (micro-optimizations), что его невозможно будет поддерживать?
3. Data Integrity: Не жертвуем ли мы точностью данных ради скорости там, где это недопустимо?
4. Graceful Degradation: Если оптимизация "провалится" — упадёт ли приложение или просто будет работать медленнее?

---

ANTI-PATTERNS (что НЕ делать):

❌ Premature optimization без измерений
❌ Кэширование всего подряд (memory bloat)
❌ Async everywhere (усложнение без пользы)
❌ Micro-optimizations в некритичном коде
❌ Удаление валидации ради скорости

---

ФОРМАТ ОТЧЕТА:
Группируй предложения по балансу "Выгода / Риск":

[ОПТИМИЗАЦИЯ]
🚀 INSTANT WIN: (Чистая победа. Меняем `list` на `set` для проверки вхождения -> ускорение O(1), рисков нет).

⚖️ TRADE-OFF: (Кэширование. Ускоряет загрузку на 500ms, но жрет +50МБ RAM. Рекомендую, если у устройства >4ГБ памяти).

🧠 UX BOOST: (Технически скорость та же, но добавляем skeleton/асинхронность, чтобы юзер не думал, что всё зависло).

📊 NEEDS MEASUREMENT: (Подозрение на проблему, но нужен профилинг для подтверждения).

🚫 ANTI-PATTERN: (Увидел в коде "грязный хак" ради скорости, который нужно убрать, так как он опасен).

---

📦 РЕЗУЛЬТАТ: Отчет оптимизации с приоритезированными рекомендациями.

🔗 СВЯЗАННЫЕ WORKFLOWS:

- Для архитектурных проблем: /audit
- Для UX оптимизаций: /UI
- После изменений: /check (верификация)

---

## 📝 VAULT LOGGING (выполни после завершения)

После завершения оптимизации обнови vault:

1. **Добавь запись в `MIND/optimization/log.md`** (сверху файла):

```markdown
---
## [ТЕКУЩАЯ ДАТА ВРЕМЯ]

**Статус:** [✅ Оптимизировано / ⚠️ Нужны trade-offs / ❌ Прерван]

**Найдено:**
- 🚀 INSTANT WIN: X
- ⚖️ TRADE-OFF: Y
- 🧠 UX BOOST: Z
- 🚫 ANTI-PATTERN: W

**Применено оптимизаций:** N из M

**Краткий итог:**
[1-2 предложения]
```

2. **Обнови `MIND/index.md`:**
   - Таблица: `/optimization` → дата, статус
   - Лог Активности: `[ДАТА] /optimization: N wins применено`

3. **Вернись в `/main`** для выбора следующего действия.
