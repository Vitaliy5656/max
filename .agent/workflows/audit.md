---
description: Глубокий аудит
---

Действуй как Lead System Architect, Security Auditor и QA Automation Engineer.
Проведи глубокий аудит (Deep Code Audit) всего контекста проекта. Ищи не только явные ошибки, но и скрытые проблемы архитектуры, логики и UX.

Игнорируй стиль кода. Фокусируйся на том, что может сломаться, зависнуть или запутать пользователя.

⏱️ ОЖИДАЕМОЕ ВРЕМЯ: 20-40 мин на модуль

---

ПРИОРИТЕТ НАХОДОК:

| Приоритет | Описание | Действие |
|-----------|----------|----------|
| P0 CRITICAL | Crash, потеря данных, security breach | Исправить немедленно |
| P1 HIGH | Silent fail, утечки памяти, race conditions | Исправить до релиза |
| P2 MEDIUM | UX-проблемы, лаги, неудобства | Запланировать |
| P3 LOW | Код-стайл, minor improvements | Backlog |

---

Анализируй последовательно в 5 этапов:

ЭТАП 1: ПОТОК ДАННЫХ И АРХИТЕКТУРА

1. Trust Boundaries: Где данные входят без валидации? (API, поля ввода, файлы, десериализация).
2. Инкапсуляция: Где нарушены границы модулей? Нет ли спагетти-кода? Есть ли circular dependencies?
3. Зависимости: Нет ли изобретения велосипедов там, где нужны стандартные библиотеки?
4. Configuration: Захардкожены ли настройки? Можно ли их менять без перекомпиляции?

---

ЭТАП 1.5: SECURITY BASICS

1. Secrets: Хранятся ли пароли/токены/API-ключи в открытом виде? (Должны быть в env/secrets manager)
2. Injection: Есть ли SQL-инъекции (string concatenation), Command Injection, Path Traversal?
3. Logging: Не логируются ли sensitive данные (пароли, токены, персональные данные)?
4. HTTPS: Все ли внешние запросы идут по защищённому каналу?

---

ЭТАП 2: ВНУТРЕННЯЯ ЛОГИКА И РЕСУРСЫ

1. State Management: Есть ли риск Race Conditions (гонки) при обновлении переменных из разных потоков? Используется ли lock/mutex где нужно?
2. Утечки (Leaks): Найди незакрытые ресурсы (таймеры, сокеты, подписки на события, IDisposable), которые "текут" при долгой работе.
3. Эффективность: Нет ли вложенных циклов O(n²) или тяжелых операций, блокирующих основной поток?
4. Error Handling: Не проглатываются ли exceptions? Есть ли fallback для критичных операций?

---

ЭТАП 3: UI/UX ЛОГИКА И ОТЗЫВЧИВОСТЬ (FRONTEND CHECK)
Проверь слой взаимодействия с пользователем (будь то Web, WPF, WinForms или CLI):

1. "Мертвые" элементы: Найди кнопки/контролы без обработчиков событий (Command/Click) или ведущие в пустые методы.
2. Синхронизация состояний (State Sync):
   - Блокируются ли кнопки во время выполнения операции (чтобы нельзя было нажать "Сохранить" дважды)?
   - Есть ли индикация загрузки (спиннеры, прогресс-бары) при долгих процессах?
   - Сбрасывается ли UI в исходное состояние при ошибке?
3. Защита ввода: Есть ли маски/валидация прямо в UI (например, ввод букв в числовое поле)?
4. Keyboard Navigation: Работает ли Tab order? Есть ли горячие клавиши?

---

ЭТАП 4: АДАПТИВНЫЙ КРАШ-ТЕСТ (CONTEXT-AWARE CHAOS)
Определи тип проекта и смоделируй 3 сценария отказа:

1. Векторы атаки (выбери подходящие): Сеть, Файловая система, Периферия (камеры, порты), Пользователь.
2. СЦЕНАРИИ (для этого кода):
   - Сценарий А (Input): Null, NaN, спецсимволы, текст гигантского размера, пустые строки.
   - Сценарий Б (Environment): Обрыв связи/USB, диск переполнен, приложение свернули/развернули, sleep/wake.
   - Сценарий В (Logic): Беспорядочное нажатие кнопок (Monkey Test), отмена операции на середине, двойной клик.

ВОПРОС: Как поведет себя код? (Crash / Freeze / Silent Fail / Graceful Handling)?

---

QUICK AUDIT CHECKLIST (для быстрой проверки):

```
☐ Null checks на входных данных
☐ Using/Dispose для IDisposable ресурсов
☐ CancellationToken для async операций
☐ Try-catch не глотает ошибки (есть logging)
☐ Нет магических чисел (используются константы)
☐ Нет закомментированного кода
☐ Публичные методы документированы
```

---

ФОРМАТ ОТЧЕТА:

[ФАЙЛ/МОДУЛЬ]

- 🔴 P0 CRITICAL: (Крэши, мертвые кнопки, потеря данных) -> Суть + Фикс.
- 🟠 P1 HIGH: (Race conditions, утечки, silent fails) -> Суть + Фикс.
- 🟡 P2 MEDIUM: (UX-проблемы, отсутствие фидбека, лаги) -> Риск + Решение.
- 🔵 P3 LOW: (Улучшения, рефакторинг) -> Предложение.

---

📦 РЕЗУЛЬТАТ: FIXES_PLAN.md с категоризированными проблемами и рекомендациями.

🔗 СВЯЗАННЫЕ WORKFLOWS:

- Для семантического анализа: /logic
- После исправлений: /check (верификация)
- Для UX проблем детально: /UI
- Для астрономических проектов: /astrodoctor

---

## 📝 VAULT LOGGING (выполни после завершения)

После завершения аудита обнови vault:

1. **Добавь запись в `MIND/audit/log.md`** (сверху файла):

```markdown
---
## [ТЕКУЩАЯ ДАТА ВРЕМЯ]

**Статус:** [✅ Чисто / ⚠️ N issues / ❌ Прерван]
**Модули проверены:** [список]
**Issues:** P0: X, P1: Y, P2: Z, P3: W

**Краткий итог:**
[1-2 предложения]
```

2. **Обнови `MIND/index.md`:**
   - Таблица: `/audit` → дата, статус, ссылка на лог
   - Секция "Активные Issues": добавь найденные P0/P1
   - Лог Активности: `[ДАТА] /audit завершён — N issues`

3. **Вернись в `/main`** для выбора следующего действия.
