---
description: Очистка от мусора
---

Действуй как Lead Technical Maintainer и Release Manager.
Твоя задача — провести генеральную уборку проекта (Repository Hygiene) и верификацию документации.

Твой девиз: "Актуальность, Чистота, Безопасность". Ничего не удаляй сам — только помечай.

⏱️ ОЖИДАЕМОЕ ВРЕМЯ: 30-60 мин на проект

---

РЕКОМЕНДУЕМАЯ ЧАСТОТА:

| Тип проверки | Частота |
|--------------|---------|
| Полный аудит | 1 раз в квартал |
| Dependency check | Перед каждым релизом |
| Docs sync | После каждого major изменения |
| Security scan | Еженедельно (автоматизировать!) |

---

Проведи ревизию в 5 этапов:

ЭТАП 1: СИНХРОНИЗАЦИЯ ДОКУМЕНТАЦИИ (DOCS vs CODE REALITY)
Просканируй все Readme, Wiki, комментарии и docstrings. Сравни их с реальным кодом.

1. "Галлюцинации" в документации: Описаны ли функции/параметры, которые уже удалены или переименованы в коде?
2. Устаревшие примеры: Взгляни на примеры кода (Usage examples) в документации. Запустятся ли они с ТЕКУЩЕЙ версией кода? (Сравни сигнатуры методов).
3. Версионирование: Везде ли указана одинаковая версия проекта (в заголовках, в package.json/csproj, в конфигах)?
4. Changelog: Актуален ли CHANGELOG.md? Отражает ли он последние изменения?

---

ЭТАП 2: ОХОТА НА МУСОР (DETRITUS HUNTER)
Найди файлы и код, которые занимают место, но не несут пользы:

1. "Зомби-код": Найди большие закомментированные блоки кода (>10 строк). Git помнит всё, хранить мусор в файлах не нужно.
2. "Сироты" (Orphaned Assets): Найди картинки, скрипты или конфиги, на которые НЕТ ссылок ни в одном файле проекта.
3. Временные артефакты: Логи (.log), кэши, временные файлы ОС (.DS_Store, Thumbs.db), которые забыли добавить в .gitignore.
4. Backup-файлы: *.bak,*.old, *_copy, file(1).ext — часто забывают удалить.
5. Debug код: `console.log`, `Debug.WriteLine`, `print()` — оставшиеся после отладки.

---

ЭТАП 2.5: GITIGNORE ЗДОРОВЬЕ

1. Всё ли что нужно игнорируется? (bin/, node_modules/, __pycache__/, .env)
2. Нет ли случайно закоммиченных secrets/.env файлов в истории? (git log -p -- .env)
3. Нет ли огромных бинарных файлов в репозитории? (git rev-list --objects --all | sort -k 2)

---

ЭТАП 3: ЗДОРОВЬЕ ЗАВИСИМОСТЕЙ (DEPENDENCY HEALTH)
Проверь файлы зависимостей (requirements.txt, package.json, nuget):

1. Deprecated: Есть ли явно заброшенные библиотеки (последний релиз >2 лет назад)?
2. Версии: Есть ли несоответствие версий (в одном месте библиотека v1.0, в другом v2.0)?
3. Unused: Есть ли библиотеки, которые подключены, но нигде не импортируются/не используются в коде?
4. License Compliance: Нет ли библиотек с несовместимыми лицензиями (GPL в коммерческом проекте)?
5. Vulnerabilities: Проверить на известные CVE:

```powershell
# Python
pip install pip-audit
pip-audit

# NPM  
npm audit

# .NET (Legacy/Optional)
dotnet list package --vulnerable
```

---

ЭТАП 4: ПЛАН ЧИСТКИ (ACTION PLAN)
Сформируй отчет в формате таблицы действий:

КАТЕГОРИЯ: [ДОКУМЕНТАЦИЯ / КОД / ФАЙЛЫ / DEPENDENCIES]

- ❌ УДАЛИТЬ: (Точно мусор. Например: "image_old.png — нигде не используется, 2.5MB").
- ⚠️ ОБНОВИТЬ: (Документация врет. Например: "Readme говорит вызвать Init(), а в коде теперь InitializeAsync()").
- 🔒 SECURITY: (Риск безопасности. Например: "API key найден в config.json").
- ℹ️ ПРОВЕРИТЬ: (Подозрительно, но нужно решение человека. Например: "Файл script_v2.py не используется, но выглядит важным").

---

ИНСТРУМЕНТЫ АВТОМАТИЗАЦИИ:

```powershell
# Поиск больших файлов (Cross-platform)
Get-ChildItem -Recurse | Where-Object {$_.Length -gt 1MB} | Sort-Object Length -Descending

# Поиск TODO/FIXME/HACK (Ripgrep recommended)
rg "TODO|FIXME|HACK"

# Python formatters
# pip install black isort
# black . --check
# isort . --check-only

# JS/TS linter
# npm run lint
```

---

📦 РЕЗУЛЬТАТ: CLEANUP_CHECKLIST.md с категоризированными действиями.

🔗 СВЯЗАННЫЕ WORKFLOWS:

- После уборки: /check (верификация что ничего не сломали)
- Для ускорения: /optimization
- Для full audit: /audit

---

## 📝 VAULT LOGGING (выполни после завершения)

После завершения уборки обнови vault:

1. __Добавь запись в `MIND/clean/log.md`__ (сверху файла):

```markdown
---
## [ТЕКУЩАЯ ДАТА ВРЕМЯ]

**Статус:** [✅ Чисто / ⚠️ Найден мусор / ❌ Прерван]

**Результаты:**
- ❌ УДАЛИТЬ: X элементов
- ⚠️ ОБНОВИТЬ: Y элементов
- 🔒 SECURITY: Z элементов
- ℹ️ ПРОВЕРИТЬ: W элементов

**Краткий итог:**
[1-2 предложения]
```

2. __Обнови `MIND/index.md`:__
   - Таблица: `/clean` → дата, статус
   - Лог Активности: `[ДАТА] /clean завершён`

3. __Вернись в `/main`__ для выбора следующего действия.
