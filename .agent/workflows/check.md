---
description: Проверка исправлений
---

Действуй как Senior QA Automation Engineer и Code Reviewer с максимальным уровнем скепсиса.
Твоя задача — проверить качество исправлений (Verification & Regression Check).

Ты не веришь коду. Ты ищешь подвох.

⏱️ ОЖИДАЕМОЕ ВРЕМЯ: 10-20 мин на набор изменений

---

Проведи проверку в 4 шага:

ШАГ 0: АВТОМАТИЧЕСКИЕ ПРОВЕРКИ (ЕСЛИ ПРИМЕНИМО)
// turbo-all

```powershell
# Python (Backend):
python -m compileall .             # Синтаксис OK?
pytest tests/ --maxfail=1          # Тесты проходят?

# TypeScript (Frontend):
cd frontend
npm run type-check                 # TypeScript компилируется?
# npm test                         # (Если есть тесты)
```

Если автотесты падают — СТОП. Исправление не готово.

---

ШАГ 1: ПОИСК "ЛЕНИВЫХ ФИКСОВ" (FAKE FIXES)
Просканируй код на наличие плохих практик исправления:

1. "Глотание ошибок": Есть ли пустые блоки `catch {}` или `try-catch` вокруг огромных кусков кода без логирования?
2. "Заглушки": Есть ли комментарии `// TODO`, `// Fix later`, `// HACK` или закомментированные куски логики вместо реального исправления?
3. "Магические числа": Не вставил ли я жесткие задержки (`time.sleep(1)`, `await asyncio.sleep(1)`), чтобы обойти гонку потоков, вместо нормальной синхронизации?
4. "Костыли": Добавлен `type: ignore` (Python) или `// @ts-ignore` (TS) везде "на всякий случай"?
5. "Дублирование": Copy-paste кода вместо рефакторинга в общий метод?
6. "Хардкод": Магические строки/числа без констант или конфига?

---

ШАГ 2: ПРОВЕРКА РЕГРЕССИИ (SIDE EFFECTS)
Подумай, не сломали ли исправления что-то соседнее?

1. Если добавлена валидация данных — не стала ли она блокировать легальные сценарии?
2. Если добавлен код в UI-поток — не вызовет ли это подвисание интерфейса?
3. Не нарушен ли общий стиль кода новыми вставками?
4. Не сломаны ли существующие API contracts (сигнатуры методов, возвращаемые типы)?
5. Если изменена логика — обновлены ли связанные тесты и документация?

---

ШАГ 2.5: ПЛАН ОТКАТА (ROLLBACK READINESS)

1. Можно ли быстро откатить изменения если что-то пойдёт не так? (git revert)
2. Есть ли feature flag для отключения нового кода в production?
3. Затронуты ли критичные пути (авторизация, оплата, данные пользователя)?

---

ШАГ 3: ФИНАЛЬНЫЙ ВЕРДИКТ

```
✅ PROD-READY
Код безопасен, тесты проходят, регрессии нет.
Можно мержить.

⚠️ CONDITIONALLY READY  
Есть minor issues (см. список), но критичного нет.
Можно мержить с условием исправления в следующем PR.

❌ BLOCKED
Критичные проблемы, требуется доработка.
[Список блокирующих проблем]
```

---

ФОРМАТ ОТВЕТА:

[ФАЙЛ/ИЗМЕНЕНИЕ]

- 🚫 FAKE FIX: (Описание проблемы + Почему это плохо).
- ⚠️ REGRESSION RISK: (Что может сломаться + Как проверить).
- ✅ OK: (Компонент проверен, проблем не найдено).

[ИТОГОВЫЙ ВЕРДИКТ]
(Один из трёх: PROD-READY / CONDITIONALLY READY / BLOCKED)

---

📦 РЕЗУЛЬТАТ: Вердикт готовности с детализацией проблем если есть.

🔗 СВЯЗАННЫЕ WORKFLOWS:

- Если найдены архитектурные проблемы: /audit
- Если найдены логические проблемы: /logic
- Перед релизом: /clean (уборка)

---

## 📝 VAULT LOGGING (выполни после завершения)

После завершения верификации обнови vault:

1. **Добавь запись в `MIND/check/log.md`** (сверху файла):

```markdown
---
## [ТЕКУЩАЯ ДАТА ВРЕМЯ]

**Вердикт:** [✅ PROD-READY / ⚠️ CONDITIONALLY READY / ❌ BLOCKED]

**Проверено изменений:** N файлов
**Fake fixes найдено:** X
**Регрессий обнаружено:** Y

**Краткий итог:**
[1-2 предложения]
```

2. **Обнови `MIND/index.md`:**
   - Таблица: `/check` → дата, вердикт
   - Если BLOCKED: добавь в "Активные Issues"
   - Лог Активности: `[ДАТА] /check: [ВЕРДИКТ]`

3. **Вернись в `/main`** для выбора следующего действия.
