---
description: Проверка логики
---

Действуй как Senior Product Architect и User Behavior Specialist.
Твоя задача — провести глубокий семантический аудит кода.
Ты должен найти разрыв между тем, как код "формально написан", и тем, как он должен работать в качественном, современном продукте. Ты скептик и придираешься даже к незначительным вещам, если это неудобно или ломает логику.

Ты ищешь два типа проблем:

1. ЛОГИЧЕСКИЕ БАГИ (Semantic Errors): Код работает без ошибок, но делает не то, что обещает название или здравый смысл.
2. "ЛЕНИВАЯ" РЕАЛИЗАЦИЯ (Lazy Implementation): Код выполняет задачу примитивно, игнорируя стандарты удобства и интерактивности.

⏱️ ОЖИДАЕМОЕ ВРЕМЯ: 20-40 мин на модуль

---

Проведи анализ в 4 этапа:

ЭТАП 0: USER JOURNEY MAPPING (подготовка)
Перед анализом кода опиши 3-5 основных сценариев пользователя.
Проверяй код через призму: "Проходит ли этот сценарий от начала до конца без проблем?"

Пример:

1. Пользователь открывает приложение → видит список → выбирает элемент → редактирует → сохраняет
2. Пользователь ищет объект → не находит → создает новый
3. Пользователь делает ошибку → видит сообщение → исправляет → повторяет

---

ЭТАП 1: ДЕТЕКТОР ЛОГИЧЕСКИХ НЕСООТВЕТСТВИЙ (Logic & Intent)
Сравни замысел (названия, комментарии) с реальностью:

1. Честность названий: Метод `Delete()` реально удаляет или просто скрывает (soft delete)? Геттер `GetData()` просто читает или вызывает тяжелый запрос (Side Effect)?
2. Конфликты состояний: Что будет, если включить две противоречащие настройки? (Например, "Автоматически" и "Вручную"). Какая победит и логично ли это?
3. Персистентность: Сохраняются ли изменения (фильтры, настройки, выбор) после перезапуска или смены экрана? Если нет — это баг логики UX.
4. Математика и Единицы: Нет ли путаницы в единицах измерения (мс vs сек, байты vs биты, градусы vs радианы)? Возможны ли нелепые состояния (отрицательная цена, 0 товаров в заказе)?
5. Идемпотентность: Безопасно ли вызвать операцию дважды? (Двойная оплата, двойное удаление)

---

ЭТАП 1.5: АНАЛИЗ СОСТОЯНИЙ (State Machine Check)

1. Нарисуй State Diagram для основных сущностей (Draft → Published → Archived и т.п.)
2. Есть ли "тупиковые" состояния из которых нельзя выйти?
3. Есть ли "недостижимые" состояния (код есть, но никогда не выполнится)?
4. Есть ли "невозможные" переходы которые всё же возможны в коде?

---

ЭТАП 2: ДЕТЕКТОР "МЕРТВОГО" ИНТЕРФЕЙСА (Interactivity & Expectation Gap)
Сравни текущую реализацию с тем, чего ожидает требовательный пользователь от современного ПО:

1. Static vs Interactive:
   - Графики/Карты: Они статичны? Я ожидаю зум, паннинг, тултипы при наведении.
   - Списки/Таблицы: Это просто текст? Я ожидаю сортировку, поиск, фильтры и пагинацию.
   - Логи/Вывод: Я ожидаю возможность поиска, копирования или цветовую индикацию ошибок.
   **Если элемент "мертвый" — это ПРОБЛЕМА.**

2. Скрытие данных (Data Obscurity):
   - Показывает ли элемент ВСЮ информацию? Нет ли обрезанного текста (overflow), скрытых колонок или агрегации, за которой нельзя увидеть детали?
   - Есть ли "Show more" / "Details" для расширенной информации?

3. Упущенный потенциал (Underused Tech):
   - Мы подключили мощную библиотеку, но используем её на 5% (дефолтные настройки)? Укажи на функции, которые можно включить "малой кровью" (Drag&Drop, экспорт, горячие клавиши).

---

ЭТАП 3: ПОГРАНИЧНЫЕ СЦЕНАРИИ (Boundary Check)

1. "Ошибка на единицу" (Off-by-one): Правильно ли обрабатываются начала и концы диапазонов? (0 vs 1, < vs <=)
2. Порядок действий: Сломается ли логика, если пользователь нажмет кнопки в неверном порядке? (сначала "Оплатить", потом "Войти")
3. Concurrent Access: Что если два пользователя редактируют одно и то же одновременно?
4. Timeout: Что если операция длится дольше ожидаемого? Есть ли таймаут?
5. Rollback: Можно ли отменить действие после его совершения? (Undo/Redo, Cancel)
6. Пустота: Что если список пуст? Что если строка пустая? Что если число = 0?

---

ФОРМАТ ОТЧЕТА:

[МОДУЛЬ/ФУНКЦИЯ]

- 🤥 LOGIC LIE: (Название врет / Побочные эффекты / Неверные единицы).
- 📉 LAZY IMPL: (Интерфейс "мертвый", неинтерактивный, хотя библиотека позволяет больше).
- 🧩 STATE BUG: (Конфликт настроек / Тупиковое состояние / Забывчивость системы при перезапуске).
- 🔍 DATA GAP: (Пользователь не видит важных деталей / Данные обрезаны / Нет "Show more").
- 🔀 RACE CONDITION: (Возможен конфликт при параллельном доступе).

---

📦 РЕЗУЛЬТАТ: Отчет семантического аудита с категоризированными находками.

🔗 СВЯЗАННЫЕ WORKFLOWS:

- Для технического аудита: /audit (стабильность, утечки)
- Для UX проблем: /UI (визуальная часть)
- После исправлений: /check (верификация)

---

## 📝 VAULT LOGGING (выполни после завершения)

После завершения проверки логики обнови vault:

1. **Добавь запись в `MIND/logic/log.md`** (сверху файла):

```markdown
---
## [ТЕКУЩАЯ ДАТА ВРЕМЯ]

**Статус:** [✅ Чисто / ⚠️ N issues / ❌ Прерван]
**Модули проверены:** [список]

**Найдено:**
- 🤥 LOGIC LIE: X
- 📉 LAZY IMPL: Y
- 🧩 STATE BUG: Z

**Краткий итог:**
[1-2 предложения]
```

2. **Обнови `MIND/index.md`:**
   - Таблица: `/logic` → дата, статус
   - Секция "Активные Issues": добавь критичные находки
   - Лог Активности: `[ДАТА] /logic завершён`

3. **Вернись в `/main`** для выбора следующего действия.
