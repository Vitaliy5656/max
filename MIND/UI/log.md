---
## 2025-12-15 18:14 — ДЕТАЛЬНЫЙ ФУНКЦИОНАЛЬНЫЙ АУДИТ

**Статус:** ⚠️ 60+ функций проанализировано
**Область:** Все хуки (useChat, useUI, useConversations, useModels, useAgent, useMetrics, useResearch) + App.tsx handlers + ResearchLab components + API client

**Найдено:**
- 🔴 CRITICAL: 7
- 🟡 UX FAIL: 12
- 💡 IMPROVEMENT: 18

---

## 📊 СВОДНАЯ ТАБЛИЦА ФУНКЦИЙ

### api/client.ts (17 функций)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `streamChat()` | 🟡 | Нет retry логики при 5xx ошибках |
| `getConversations()` | 💡 | Нет пагинации (limit=50 захардкожен) |
| `createConversation()` | ✅ | Работает корректно |
| `getMessages()` | 💡 | limit=100 может быть недостаточно |
| `getDocuments()` | ✅ | OK |
| `uploadDocument()` | 🔴 | Нет progress callback для больших файлов |
| `deleteDocument()` | ✅ | OK |
| `getTemplates()` | ✅ | OK |
| `createTemplate()` | ✅ | OK |
| `startAgent()` | 🟡 | Нет cancellation token |
| `getAgentStatus()` | 🟡 | Polling вместо WebSocket (неэффективно) |
| `stopAgent()` | ✅ | OK |
| `getMetrics()` | ✅ | OK |
| `getAchievements()` | ✅ | OK |
| `submitFeedback()` | ✅ | OK |
| `getModels()` | ✅ | OK |
| `checkHealth()` | ✅ | OK |

---

### useUI.ts (6 функций/состояний)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `activeTab` state | 🟡 | Не сохраняется в URL (deeplink impossible) |
| `setActiveTab()` | 💡 | Нет анимации перехода |
| `sidebarOpen` state | 🟡 | Не персистится в localStorage |
| `toggleSidebar()` | ✅ | OK |
| `addLog()` | 💡 | Логи не ограничены (memory leak при длительной сессии) |
| `handleResize()` | ✅ | Корректно определяет mobile breakpoint |

**CRITICAL useUI:**

- 🔴 **`addLog()`**: `setSystemLogs(prev => [...prev, newLog])` — массив растёт бесконечно. После 1000+ логов будет OOM.
  - **FIX:** `setSystemLogs(prev => [...prev.slice(-100), newLog])`

---

### useChat.ts (10 функций)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `formatTime()` | ✅ | OK |
| `sendMessage()` | 🔴 | Нет debounce — двойной клик = два запроса |
| `stopGeneration()` | ✅ | AbortController работает |
| `clearMessages()` | 🟡 | Нет confirmation dialog |
| `loadMessages()` | 💡 | Нет loading skeleton |
| `toggleThinkExpanded()` | ✅ | OK |
| token callback | 🟡 | Нет подсчёта tokens/sec |
| thinking callback | ✅ | Хорошо структурировано |
| confidence callback | ✅ | OK |
| queue callback | ✅ | OK |

**CRITICAL useChat:**

- 🔴 **`sendMessage()`** строка 66: `if (!input.trim() || isGenerating) return;`
  - Защита есть, НО: кнопка в InputArea не блокируется мгновенно → race condition при быстром double-click
  - **FIX:** Добавить `disabled` state ПЕРЕД async операцией

---

### useConversations.ts (4 функции)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `loadConversations()` | 💡 | console.error вместо UI toast |
| `createConversation()` | 🟡 | Нет error handling (throws unwrapped) |
| `selectConversation()` | ✅ | OK |
| `setConversationId()` | ✅ | OK |

**CRITICAL useConversations:**

- 🔴 **`createConversation()`**: `const conv = await api.createConversation()` — если API упал, exception не перехвачен, UI зависает.
  - **FIX:** Wrap in try/catch, add UI error state

---

### useModels.ts (6 функций)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `loadModels()` | 💡 | Silently fails — пользователь не знает |
| `loadModelSelectionMode()` | 💡 | Hardcoded URL (не использует API_BASE) |
| `updateModelSelectionMode()` | 🟡 | Optimistic UI без rollback при ошибке |
| `getModelDisplayName()` | ✅ | OK |
| `setSelectedModel()` | ✅ | OK |
| `setThinkingMode()` | ✅ | OK |

**CRITICAL useModels:**

- 🔴 **`loadModelSelectionMode()`** строка 46: `fetch('http://localhost:8000/...')`
  - Hardcoded URL не работает в production! Должен использовать `API_BASE`
  - **FIX:** Import API_BASE from client.ts

---

### useAgent.ts (5 функций)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `requestAgent()` | ✅ | Показывает confirmation modal |
| `confirmAgent()` | 🟡 | Polling каждые 2сек — неэффективно |
| `cancelAgent()` | 🟡 | Только закрывает modal, не отменяет task |
| `setAgentGoal()` | ✅ | OK |
| polling logic | 🔴 | clearInterval не вызывается при unmount |

**CRITICAL useAgent:**

- 🔴 **Polling interval leak**: `setInterval` в `confirmAgent()` не очищается при размонтировании компонента.
  - **FIX:** Сохранять intervalId в ref и очищать в useEffect cleanup

---

### useMetrics.ts (1 функция)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `loadMetrics()` | 💡 | console.error вместо UI feedback |

---

### useResearch.ts (15+ функций)

| Функция | UX Оценка | Проблемы |
|---------|-----------|----------|
| `connectWebSocket()` | 🔴 | Нет connectionStatus state для UI |
| `onopen()` | ✅ | Сбрасывает error |
| `onmessage()` | ✅ | Хорошо парсит разные типы событий |
| `onclose()` | 🟡 | Reconnect без индикации пользователю |
| `onerror()` | 💡 | Только console.error |
| `fetchTopics()` | ✅ | OK |
| `fetchTasks()` | ✅ | OK |
| `startResearch()` | 🟡 | 409 Conflict не объясняет какой именно research уже запущен |
| `cancelResearch()` | ✅ | Имеет window.confirm |
| `refreshTopic()` | ✅ | OK |
| `deleteTopic()` | ✅ | Имеет window.confirm |
| `searchTopic()` | ✅ | OK |
| `getTopicSkill()` | ✅ | OK |
| `addToQueue()` | 🟡 | Нет Undo toast |
| `removeFromQueue()` | ✅ | OK |
| `pauseQueueItem()` | ✅ | OK |
| `resumeQueueItem()` | ✅ | OK |
| `moveQueueUp/Down()` | ✅ | OK |
| `reResearchTopic()` | ✅ | OK |
| `calculateQuality()` | 💡 | Magic numbers не объяснены в UI |

---

## 📋 App.tsx HANDLERS (12 функций)

| Handler | UX Оценка | Проблемы |
|---------|-----------|----------|
| `loadInitialData()` | 💡 | Promise.all — одна ошибка ломает всё |
| `handleSendMessage()` | ✅ | Делегирует в useChat |
| `handleNewConversation()` | 💡 | Вызывает loadConversations после create (дублирование) |
| `handleFeedback()` | ✅ | OK |
| `handleRegenerate()` | 🔴 | Удаляет сообщение, но НЕ отправляет новое |
| `handleCopy()` | ✅ | OK |
| `handleUploadDocument()` | 🟡 | Нет upload progress bar |
| `handleDeleteDocument()` | 💡 | Модалка с подтверждением — хорошо |
| `handleUseTemplate()` | ✅ | OK |
| `handleClickOutside()` | ✅ | OK |
| filteredTemplates | ✅ | useMemo не используется (micro-optimization) |

---

## 🧩 ResearchLab.tsx COMPONENTS (12 компонентов)

| Компонент | UX Оценка | Проблемы |
|-----------|-----------|----------|
| `getStageIndex()` | ✅ | OK |
| `formatETA()` | ✅ | OK |
| `QualityBar` | ✅ | Хорошая визуализация |
| `StatsDashboard` | 🟡 | "Система готова" — статичный текст |
| `ActivityFeed` | ✅ | OK |
| `ResearchQueuePanel` | ✅ | Отличная UX с drag-reorder |
| `ResearchStepper` | ✅ | Понятные этапы |
| `TaskCard` | ✅ | OK |
| `TopicCard` | 💡 | Кнопки появляются только при hover (не accessible) |
| `EmptyState` | 🟡 | Английский текст (локализация!) |
| `SkillModal` | ✅ | OK |
| `CelebrationModal` | ✅ | Отличный UX — празднование успеха |
| `handleViewSkill()` | ✅ | OK |

---

## 🎯 ПРИОРИТЕТ ИСПРАВЛЕНИЙ (ФУНКЦИОНАЛЬНЫЙ)

### P0 — Must Fix (блокирующие баги)

1. **useAgent interval leak** — memory leak, CPU waste
2. **useModels hardcoded URL** — сломано в prod
3. **useConversations no error handling** — UI freezes
4. **uploadDocument no progress** — плохой UX для больших файлов
5. **handleRegenerate broken flow** — не завершает action
6. **addLog memory leak** — OOM после длительной сессии
7. **sendMessage double-click** — дублирование запросов

### P1 — Should Fix (UX проблемы)

1. activeTab не в URL (no deeplinks)
2. sidebarOpen не персистится
3. WebSocket reconnect без индикации
4. Polling вместо WebSocket для Agent
5. English text в EmptyState
6. TopicCard buttons не accessible без hover

### P2 — Nice to Have (улучшения)

1. Tokens/sec counter при генерации
2. Loading skeletons везде
3. Toast notifications вместо console.error
4. Upload progress bar
5. Animated tab transitions

---

## 2025-12-15 18:10

**Статус:** ⚠️ 16 issues
**Экраны проверены:** Chat (useChat, MessageBubble, ThinkingPanel, InputArea), DeepSearch/ResearchLab, Переключение вкладок (useUI)

**Найдено:**

- 🔴 CRITICAL: 3
- 🟡 UX FAIL: 5
- 💡 IMPROVEMENT: 8

**Краткий итог:**
Аудит сфокусирован на логике чатов, модуле DeepSearch (Research Lab), и возвращении состояния окон при переключении. Обнаружены критические проблемы с потерей состояния чата при смене вкладок, отсутствием сохранения черновиков, и невозможностью отменить отправку сообщения. ResearchLab имеет хорошую архитектуру, но нуждается в улучшении обратной связи при ошибках WebSocket.

---

### 1. ЛОГИКА ЧАТОВ (useChat.ts, App.tsx, MessageBubble.tsx)

#### 🔴 CRITICAL: Потеря состояния thinkingSteps между запросами

**Файл:** `useChat.ts:132`

```typescript
setThinkingSteps([]); // Clear previous steps
```

**Проблема:** При начале нового запроса `thinkingSteps` очищается. Если пользователь хочет сравнить мыслительный процесс для разных ответов — это невозможно. Состояние привязано к текущему запросу, а не к сообщению.
**Рекомендация:** Сохранять `thinkingSteps` в объекте `Message` как `thinking: string[]` для каждого сообщения ассистента.

#### 🔴 CRITICAL: Нет восстановления состояния при сбое streaming

**Файл:** `useChat.ts:176-191`
**Проблема:** При ошибке streaming (`catch` блок) сообщение ассистента получает текст ошибки, но:

1. Нет возможности повторить запрос автоматически
2. Нет кнопки "Retry" непосредственно в UI ошибки
3. Оригинальный input пользователя НЕ сохраняется для retry
**Рекомендация:** Добавить в Message интерфейс поле `hasError: boolean` и отображать кнопку "Повторить" в MessageBubble.

#### 🟡 UX FAIL: Confusing feedback states

**Файл:** `App.tsx:82` + `MessageBubble.tsx:99-118`
**Проблема:** `feedbackSent` хранится в `App.tsx` как локальное состояние и НЕ сохраняется:

- После перезагрузки страницы все feedback сбрасывается
- При переключении между разговорами feedback смешивается (ключи — messageId, не уникальны глобально)
**Рекомендация:** Сохранять feedback в backend или localStorage с ключом `{conversationId}_{messageId}`.

#### 🟡 UX FAIL: handleRegenerate ломает историю

**Файл:** `App.tsx:182-189`

```typescript
chat.setMessages(prev => prev.filter(m => m.id !== messageId));
```

**Проблема:** При регенерации удаляется сообщение ассистента, но НЕ вызывается повторная отправка автоматически. Пользователь должен вручную нажать Enter.
**Рекомендация:** После `setInput(...)` автоматически вызывать `handleSendMessage()` или добавить confirmation dialog.

#### 💡 IMPROVEMENT: Нет индикации "печатает"

**Файл:** `ThinkingPanel.tsx`
**Проблема:** Нет визуального индикатора "MAX печатает..." (typing indicator dots) между получением первого токена и завершением ответа.
**Рекомендация:** Добавить `TypingIndicator` компонент при `isGenerating && !isThinking`.

#### 💡 IMPROVEMENT: Инкрементальный счётчик токенов

**Файл:** `useChat.ts:100-110`
**Проблема:** При стриминге токенов нет отображения:

- Количества полученных токенов
- Скорости генерации (tokens/sec)
**Рекомендация:** Добавить в UI счётчик `{tokens} tokens • {speed} tok/s`.

---

### 2. ЛОГИКА DEEPSEARCH (ResearchLab.tsx, useResearch.ts)

#### 🔴 CRITICAL: WebSocket reconnect может потерять состояние

**Файл:** `useResearch.ts:137-143`

```typescript
ws.onclose = () => {
    reconnectTimeoutRef.current = window.setTimeout(() => {
        connectWebSocket();
    }, 3000);
};
```

**Проблема:** При разрыве WebSocket происходит reconnect через 3 секунды, но:

1. Пользователь НЕ видит, что соединение потеряно (нет визуального индикатора)
2. Если за это время задача завершилась — прогресс-бар застрянет
3. После reconnect запрашивается `initial_state`, но UI может не обновиться корректно
**Рекомендация:** Добавить `connectionStatus: 'connected' | 'disconnected' | 'reconnecting'` в return useResearch и отображать в UI.

#### 🟡 UX FAIL: No confirmation before starting expensive research

**Файл:** `ResearchLab.tsx:707-714`
**Проблема:** Нажатие "В очередь" добавляет research без подтверждения. Если пользователь случайно нажал — нет "undo".
**Рекомендация:** Добавить toast с "Undo" кнопкой (5 сек) как в Gmail.

#### 🟡 UX FAIL: Queue не восстанавливается полностью из localStorage

**Файл:** `useResearch.ts:344-352`
**Проблема:** Queue сохраняется в localStorage, но `taskId` (связь с backend) теряется. После перезагрузки:

- Показывается `running` элемент, но реального task в backend может не быть
- Нет синхронизации статуса
**Рекомендация:** При загрузке из localStorage проверять статус каждого `taskId` через API `/tasks/{id}`.

#### 💡 IMPROVEMENT: Пустой EmptyState на английском

**Файл:** `ResearchLab.tsx:417-433`

```tsx
<h3>Your Knowledge Library is empty</h3>
<p>Start your first research...</p>
```

**Проблема:** Текст на английском, интерфейс смешан (кнопки на русском "Отмена", "В очередь").
**Рекомендация:** Перевести на русский для консистентности.

#### 💡 IMPROVEMENT: StatsDashboard всегда показывает "Система готова"

**Файл:** `ResearchLab.tsx:104-106`
**Проблема:** Статус активности всегда `●` и "Система готова", не отражает реальный статус WebSocket.
**Рекомендация:** Привязать к `connectionStatus` из WebSocket.

#### 💡 IMPROVEMENT: Quality calculation непрозрачен

**Файл:** `useResearch.ts:486-490`
**Проблема:** `calculateQuality` использует магические числа (50 chunks, 0.7, 0.3) без объяснения.
**Рекомендация:** Добавить tooltip в UI объясняющий как считается качество.

---

### 3. ЛОГИКА ПЕРЕКЛЮЧЕНИЯ ОКОН (useUI.ts, App.tsx)

#### 🔴 CRITICAL: Состояние чата НЕ сохраняется при смене вкладки

**Файл:** `useUI.ts:8` + `App.tsx:420-461`
**Проблема:** При переключении с `chat` на `research` и обратно:

1. `thinkContent` и `thinkingSteps` СОХРАНЯЮТСЯ (хорошо)
2. `input` (черновик сообщения) СОХРАНЯЕТСЯ (хорошо)
3. НО: Если во время switching идёт `isGenerating` — генерация продолжается в фоне, но UI скрывает ThinkingPanel
**Рекомендация:** Показывать floating indicator "Генерация в процессе..." если `isGenerating && activeTab !== 'chat'`.

#### 🟡 UX FAIL: ResearchLab WebSocket создаётся ВСЕГДА при монтировании

**Файл:** `useResearch.ts:153-164`
**Проблема:** `connectWebSocket()` вызывается в `useEffect` сразу же, даже если пользователь никогда не заходит на вкладку Research. Это:

1. Создаёт ненужное соединение
2. Генерирует network трафик
**Рекомендация:** Lazy-инициализация: подключаться к WebSocket только при первом переключении на `activeTab === 'research'`.

#### 💡 IMPROVEMENT: Нет анимации при переключении вкладок

**Файл:** `App.tsx:420-677`
**Проблема:** Смена контента происходит мгновенно (условный рендеринг). Нет плавного fade/slide перехода.
**Рекомендация:** Добавить `animate-in fade-in` на каждый tab content wrapper или использовать `framer-motion` AnimatePresence.

#### 💡 IMPROVEMENT: Sidebar collapsed state не сохраняется

**Файл:** `useUI.ts:9`
**Проблема:** `sidebarOpen` — локальный state. После перезагрузки:

- Desktop: sidebar открывается (логично)
- Mobile: sidebar закрыт (логично)
- НО: если пользователь на desktop закрыл sidebar вручную — после перезагрузки он откроется
**Рекомендация:** Сохранять `sidebarOpen` в localStorage для non-mobile.

---

### МЕТРИКИ ВАЛИДАЦИИ

| Метрика | Оценка | Комментарий |
|---------|--------|-------------|
| Time to First Click (TTFC) | ✅ 2-3 сек | CTA очевидны |
| Error Recovery Rate | ⚠️ Средний | Нет retry для streaming ошибок |
| Predictability | ✅ Высокий | Кнопки делают то, что написано |
| State Persistence | 🔴 Низкий | Многие состояния теряются |

---

### ПРИОРИТЕТ ИСПРАВЛЕНИЙ

**P0 (Must Fix):**

1. WebSocket connection indicator в ResearchLab
2. Floating indicator при генерации в фоне
3. Retry button для streaming ошибок

**P1 (Should Fix):**
4. Сохранение feedback в backend
5. Lazy-init WebSocket для ResearchLab
6. Locale consistency (RU everywhere)

**P2 (Nice to Have):**
7. Анимации переключения вкладок
8. Token counter при генерации
9. Quality score explanation tooltip

---

## 2025-12-13 23:15

**Статус:** ✅ Чисто (с рекомендациями)
**Экраны проверены:** ChatWindow, Sidebar, InputArea, ThinkingPanel, RAG/History Tabs

**Найдено:**

- 🔴 CRITICAL: 0
- 🟡 UX FAIL: 1
- 💡 IMPROVEMENT: 4

**Краткий итог:**
Интерфейс выглядит современным и отзывчивым. Отличная реализация обратной связи через `ThinkingPanel`. Мобильная адаптация выполнена качественно. Основные рекомендации касаются расширения обработки ошибок и улучшения доступности (a11y).

---

### [App.tsx / Sidebar]

- 💡 **IMPROVEMENT**: В мобильной версии кнопка закрытия меню (`X`) перекрывает контент или находится слишком близко к краю. Рекомендуется увеличить touch area до 44px (сейчас `p-2`, но сама иконка маленькая).
- 💡 **IMPROVEMENT**: Отсутствует явный индикатор "Оффлайн" режима. Если API недоступен, пользователь видит только лог. Лучше добавить визуальный индикатор в `Header` (красная точка или иконка перечеркнутого wifi).

### [InputArea.tsx]

- 💡 **IMPROVEMENT**: При генерации ответа (`isGenerating`) кнопка превращается в "Стоп". Это хорошо, но можно добавить tooltip "Остановить генерацию" при наведении для ясности.
- 🟡 **UX FAIL**: При нажатии `Enter` отправка происходит мгновенно. Если пользователь случайно нажал Enter на середине предложения, сообщение улетит. Рекомендуется добавить небольшую задержку или возможность отмены отправки в течение 1-2 секунд (как "Undo" в Gmail), либо оставить как есть, если это чат-стандарт, но учесть это. *Correction: В чатах Enter для отправки — стандарт (Закон Якоба), поэтому это не FAIL, а особенность.*
- **Correction**: Реальный UX FAIL — это отсутствие сохранения черновика. Если пользователь набрал текст и случайно перезагрузил страницу или переключил вкладку, `useChat` может сбросить состояние (если не сохраняется в localStorage).

### [ThinkingPanel.tsx]

- ✅ **GOOD**: Отличная визуализация процесса "думания". Это снижает воспринимаемое время ожидания (Perceived Wait Time).
- 💡 **IMPROVEMENT**: Анимация `animate-spin` на иконке загрузки может утомлять при долгом ожидании (>30 сек). Можно добавить текстовую смену статусов ("Анализирую...", "Формирую ответ...", "Проверяю факты..."), чтобы пользователь видел прогресс.

### [RAG / History Tabs]

- 💡 **IMPROVEMENT**: Списки документов и истории используют `GlassCard` без пагинации. Если документов будет 100+, список станет бесконечным. Рекомендуется виртуализация списка или пагинация.

### [Accessibility (a11y)]

- 🟡 **UX FAIL**: Некоторые интерактивные элементы (например, кнопки в `MessageBubble` actions) имеют `aria-label`, но не имеют `title` (или наоборот), или контрастность иконок в неактивном состоянии (`text-zinc-500` на `bg-zinc-900`) может быть недостаточной для WCAG AA.
